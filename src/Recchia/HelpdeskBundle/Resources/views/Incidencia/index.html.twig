{% extends '::layout.html.twig' %}

{% block content -%}
    <h2>Incidencias</h2>

    <table class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>#</th>
                <th>Cédula</th>
                <th>Nombres</th>
                <th>Apellidos</th>
                <th>Incidencia</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.id }}</td>
                <td>{{ entity.cedula }}</td>
                <td>{{ entity.nombres }}</td>
                <td>{{ entity.apellidos }}</td>
                <td>{{ entity.incidencia }}</td>
                <td>
                    <a class="btn btn-default btn-sm" href="{{ path('incidencia_show', { 'id': entity.id }) }}">ver</a>
                    <a class="btn btn-default btn-sm" href="{{ path('incidencia_edit', { 'id': entity.id }) }}">editar</a>
                </td>
            </tr>
        
        {% else %}
            <tr class="success"><td colspan="8"><p class="lead text-center">No existen registros</p></td></tr>
        {% endfor %}
        </tbody>
    </table>
    <a class="btn btn-primary" href="{{ path('incidencia_new') }}">
        Nueva Incidencia
    </a>
{% endblock %}
